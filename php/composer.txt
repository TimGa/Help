# Versions
~1.2.3  # до 1.3 (не включительно)
^1.2.3  # до 2.0 (не включительно)

# json
{
    "name": "timga/factory-method",
    
    "require": {
        "php": "^7.1"
    },

    "autoload": {
        "psr-4": {
            "TimGa\\FactoryMethod\\": "src/"
        }
    }
}

# update autoload file
composer dump-autoload